language: node_js
cache:
  directories:
    - node_modules
node_js:
  - 9
  - 8
sudo: false
services:
  - couchdb
env:
  - DEBUG=couch:* REST_ON_COUCH_USERNAME=admin REST_ON_COUCH_PASSWORD=admin
before_script:
  - sh setupDatabase.sh